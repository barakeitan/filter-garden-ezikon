-- File: src/config.lua

--[[
    NOTE:
    This file contains default paths for input and output files and other configurable settings.
    It allows for easy adjustments to project behavior without altering the core code.
]]

local config = {
    inputPath = "./data/moshe_bitfield.json",
    outputPath = "./output/moshe_bitfield.lua"
}

return config